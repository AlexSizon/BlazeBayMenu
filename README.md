# BlazeBayMenu

This repository starts with a minimal OpenAPI 3.1 spec in `openapi.yaml`.

Current scope:

- project metadata
- local development server
- a simple `/health` endpoint

Suggested next steps:

1. Define the core menu resources and flows.
2. Expand `components/schemas` before adding many endpoints.
3. Add linting or documentation tooling once the contract starts to grow.
